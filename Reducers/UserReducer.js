
// Initial State
const INITIAL_STATE = {
    name: 'Ashish Gupta',
}

const UserReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default UserReducer;